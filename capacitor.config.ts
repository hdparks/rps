import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hdparks.rps',
  appName: "HDParks' Rock-Paper-Scissors",
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
